# Сопроводительное письмо — LawyerApp

**Готовый результат**

- Live demo: https://lawyerapp.sy3.ru/documents/d1
- Репозиторий: https://github.com/suyazov/lawyerapp-frontend-task

**Что сделано**

Переработан экран истории документа `/documents/[id]` на мок-данных. Основной кейс для проверки — `/documents/d1` (конфликт, просрочка, разные статусы и уровни).

Ключевые решения:

- Сводка состояния сверху — с первого взгляда видно: версия, что открыто/закрыто, блокеры, просрочка, конфликты.
- Ровная таблица правок вместо группировки по дням — нет лишней иерархии.
- Ответственный вынесен отдельной колонкой — сразу видно, кто настоял на правке.
- Конфликт по п. 7.1 оформлен группой — не теряется как две похожие строки.
- Детали правки (было/стало, аргументация, приватные заметки, Битрикс, доказательства) — в диалог по клику.
- Append-only: закрытые/отклонённые правки остаются в списке с пониженной непрозрачностью, но не конкурируют за внимание.

**Проверки**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

- Сборка проходит без ошибок.
- Линт чистый.
- TypeScript без ошибок.
- Smoke HTTPS: `/documents/d1` открывается, данные рендерятся корректно.
- Скриншоты ключевых состояний: `docs/screenshots/document-d1.png`, `docs/screenshots/document-d5.png`.

**Что не делалось**

Backend, авторизация и реальное хранилище не реализовывались — они вне скоупа по ТЗ (работаем на мок-данных, фронтенд одного экрана).
